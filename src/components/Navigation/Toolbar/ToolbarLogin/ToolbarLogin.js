import React from 'react';

import classes from './ToolbarLogin.css';

const toolbarLogin = ( props ) => (
    <header className={classes.Toolbar}>
        <p className={classes.Name}>NeverForget</p>
        <nav />
    </header>
);

export default toolbarLogin;